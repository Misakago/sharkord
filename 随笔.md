# 编程随笔：从「能跑」到「好改」

## 一、引言

每个程序员都经历过这样的时刻：深夜，盯着自己几个小时前刚写完的代码，脑子里只有一个念头——这到底是谁写的？

代码就像一面镜子，照出的是写代码那一刻的自己。状态好时，代码结构清晰、命名优雅；状态差时，连自己都不忍直视。而代码最有趣的地方在于，它不仅是写给机器执行的指令，更是写给下一个开发者（包括未来的自己）阅读的文字。

## 二、命名：编程中最难的事

计算机科学领域有一句广为流传的名言：**只有两件事是真正困难的——缓存失效和命名。** 这句话虽然带着调侃，却道出了真相。

一个好的变量名胜过十行注释。看看这两段代码：

```python
# 版本 A
d = datetime.now().strftime("%Y-%m-%d")

# 版本 B
today_date = datetime.now().strftime("%Y-%m-%d")
```

版本 B 并没有多做什么，但它让代码的意图一目了然。变量名 `today_date` 天然地说明了这个变量的含义，不需要额外注释。

但命名的问题在于，它强迫你去思考：这个东西到底是什么？它的职责边界在哪里？在写代码最急迫的时候，我们往往不愿意花这个时间，于是就有了 `data1`、`temp`、`tmp`、`flag` 这样的名字。

这本身不是技术能力的缺失，而是对思考的逃避。命名之所以难，是因为它要求你在动手之前先想清楚。

## 三、抽象：恰到好处才是好

初学编程时，我们被告知要 DRY（Don't Repeat Yourself）。于是很多人走上了过度抽象的道路。

```python
# 两次重复就抽象
def process_data(data, mode):
    if mode == "a":
        ...
    elif mode == "b":
        ...
```

最后这个函数变得臃肿不堪，参数列表越来越长，逻辑越来越复杂，调用方需要读完整段代码才知道传什么参数。这其实比最原始的复制粘贴更糟糕——因为原始的代码至少是直白的，而抽象后的代码把你对上下文的理解藏到了别处。

一个好的抽象应该满足三个条件：

1. **职责单一**：一个函数只做一件事
2. **接口直观**：调用者不需要看实现就知道怎么用
3. **易于移除**：如果哪天不需要了，删除它不会影响其他地方

如果做不到这三点，也许就不应该抽象。

## 四、注释：写为什么，而不是写是什么

初学者爱写这样的注释：

```python
# 循环遍历列表
for item in items:
    ...
```

但读代码的人难道看不出来这是一个循环吗？真正需要注释的是为什么。

```python
# 使用二分查找而不是线性遍历，因为列表已排序且数据量超过 10 万
def find_item(sorted_list, target):
    ...
```

这类注释提供了代码本身无法表达的信息：决策背后的理由。当后来者看到这段代码时，他们能理解为什么这么做，而不是在下次重构时「优化」掉一个精心设计的方案。

注释还有一个容易被忽视的作用——标记遗憾。每个项目都有不完美的地方，用 `TODO`、`HACK`、`FIXME` 这样的标记诚实地记录下为什么这里写得不够好，反而是对项目长期健康的负责。

## 五、可读性：代码首先是给人看的

写代码的时候，编译器不在乎你的代码是优美还是丑陋——它只在乎语法是否正确。但读代码的人是你在乎的。

判断代码可读性的一个简单方法：**如果你不敢在 code review 时让别人看你的代码，那它大概率不够好。**

提升可读性的几个实用技巧：

- **一致的风格**：不要在一个文件里混用驼峰和下划线，不要有的加空格有的不加。用格式化工具一次性搞定。
- **短的函数**：如果一个函数超过屏幕高度（大约 30-40 行），考虑拆分。长的函数就像没有分段落的文章，读起来让人窒息。
- **避免深层嵌套**：`if` 里面套 `if` 里面套 `if`。遇到这种情况，尽早 return 或者提取成小函数。
- **布尔值命名成断言**：`is_ready`、`has_permission`、`should_retry`，读起来像自然语言。

## 六、技术债务的利息

技术债这个概念已经被说烂了，但大多数人只关注它「是什么」，而忽略了它「怎么积累利息」。

一段糟糕的代码写成之后，它的「利息」是这样累积的：

- 第一次读它的人多花了 5 分钟
- 第一次修改它的人多花了 10 分钟（因为要理解上下文）
- 之后每次修 bug 都可能引入新 bug，因为没有人真正理解它
- 新成员加入团队时，需要花更多时间上手

这些时间看似零散，但乘以被读的次数，就是一笔巨大的成本。而最初写出这段代码省下的那 10 分钟，相比较之下微不足道。

用一句话总结：**写代码的时候偷的懒，会在未来千百倍地还回来。**

## 七、结语

编程是一门手艺，和木工、写作没有本质区别。好的手艺人不只是追求「做出来」，而是追求「做得漂亮」。代码的读者可能是你的同事、你的继任者，或者三个月后的你自己。对他们好一点，也对你未来的自己好一点。

归根结底，好的代码不是写出来的，而是改出来的。写出优雅代码的秘密不在于天赋，而在于愿意花时间去打磨它。

正如 Knuth 所说：**「程序员是写出自己能理解的代码，而优秀的程序员写出别人也能理解的代码。」**
