name: Manual Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump"
        required: true
        type: choice
        options:
          - none
          - patch
          - minor
          - major
        default: "none"

permissions:
  contents: write
  pull-requests: write

jobs:
  ci:
    uses: ./.github/workflows/ci.yml

  e2e:
    uses: ./.github/workflows/e2e.yml

  build-and-publish:
    needs: [ci, e2e]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build project artifacts
        uses: ./.github/actions/build-mikotord
        with:
          bump: ${{ inputs.bump }}

      - name: Get new version
        id: get_version
        run: echo "version=$(jq -r .version package.json)" >> "$GITHUB_OUTPUT"

      - name: Identify user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "automation@example.invalid"

      - name: Build and publish Docker image
        uses: ./.github/actions/docker-build-publish
        with:
          docker_username: ${{ secrets.DOCKER_USERNAME }}
          docker_password: ${{ secrets.DOCKER_PASSWORD }}
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/mikotord:latest
            ${{ secrets.DOCKER_USERNAME }}/mikotord:v${{ steps.get_version.outputs.version }}
          labels: |
            org.opencontainers.image.title=Mikotord
            org.opencontainers.image.description=Mikotord Server
            org.opencontainers.image.version=${{ steps.get_version.outputs.version }}

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.get_version.outputs.version }}
          name: v${{ steps.get_version.outputs.version }}
          draft: true
          files: |
            apps/server/build/out/mikotord-linux-x64
            apps/server/build/out/mikotord-linux-arm64
            apps/server/build/out/mikotord-windows-x64.exe
            apps/server/build/out/mikotord-macos-x64
            apps/server/build/out/mikotord-macos-arm64
            apps/server/build/out/release.json
          body: |
            ## Changes
            (TODO)

            ## Docker Image
            ```bash
            docker pull ${{ secrets.DOCKER_USERNAME }}/mikotord:v${{ steps.get_version.outputs.version }}
            ```
      - name: Commit version bump
        if: ${{ inputs.bump != 'none' }}
        run: |
          git add .
          git commit -m "chore: bump version to ${{ steps.get_version.outputs.version }}"

      - name: Create Git tag
        run: git tag -a "v${{ steps.get_version.outputs.version }}" -m "Release v${{ steps.get_version.outputs.version }}"

      - name: Push commit and tag
        run: git push --follow-tags
